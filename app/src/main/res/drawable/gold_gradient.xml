<?xml version="1.0" encoding="utf-8"?>
<!-- Gold sheen used only where gold is the subject: rate badge, the bill's top strip. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <gradient
        android:startColor="@color/gold_deep"
        android:centerColor="@color/gold_bright"
        android:endColor="@color/gold"
        android:angle="0" />
</shape>
